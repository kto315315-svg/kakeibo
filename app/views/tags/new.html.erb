<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>タグを追加する</h1>
      <%= link_to "一覧に戻る", tags_path, class: "btn btn-secondary" %>
    </div>
    <div class="card">
      <div class="card-body p-4">
        <%= form_with model: @tag do |f| %>
          <% if @tag.errors.any? %>
            <div class="error-messages">
              <p><%= @tag.errors.count %>件のエラーがあります</p>
              <ul>
                <% @tag.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="mb-3">
            <%= f.label :name, "タグ名", class: "form-label" %>
            <%= f.text_field :name, class: "form-control", placeholder: "例：外食" %>
          </div>
          <%= f.submit "追加する", class: "btn btn-primary w-100" %>
        <% end %>
      </div>
    </div>
  </div>
</div>