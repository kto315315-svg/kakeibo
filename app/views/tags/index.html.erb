<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>タグ</h1>
  <%= link_to "＋ 追加", new_tag_path, class: "btn btn-primary btn-sm" %>
</div>

<div class="card">
  <div class="card-body p-0">
    <% @tags.each_with_index do |tag, index| %>
      <div class="d-flex justify-content-between align-items-center px-3 py-2 <%= index != @tags.length - 1 ? 'border-bottom' : '' %>" style="border-color: rgba(255,255,255,0.05) !important;">
        <span style="font-size: 0.95rem; color: #e0e0e0;">
          🔖 <%= tag.name %>
        </span>
        <div class="d-flex gap-2">
          <%= link_to "編集", edit_tag_path(tag), class: "btn btn-sm btn-warning" %>
          <%= link_to "削除", tag_path(tag), class: "btn btn-sm btn-danger", data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" } %>
        </div>
      </div>
    <% end %>
  </div>
</div>