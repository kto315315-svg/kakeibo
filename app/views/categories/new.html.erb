<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>カテゴリを追加する</h1>
      <%= link_to "一覧に戻る", categories_path, class: "btn btn-secondary" %>
    </div>
    <div class="card">
      <div class="card-body p-4">
        <%= form_with model: @category do |f| %>
          <% if @category.errors.any? %>
            <div class="error-messages">
              <p><%= @category.errors.count %>件のエラーがあります</p>
              <ul>
                <% @category.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="mb-3">
            <%= f.label :name, "カテゴリ名", class: "form-label" %>
            <%= f.text_field :name, class: "form-control", placeholder: "例：食費" %>
          </div>
          <%= f.submit "追加する", class: "btn btn-primary w-100" %>
        <% end %>
      </div>
    </div>
  </div>
</div>